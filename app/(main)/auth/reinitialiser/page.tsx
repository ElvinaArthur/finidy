'use client'
import { FormEvent, useState } from 'react'
import { useSearchParams } from 'next/navigation'
import Link from 'next/link'
export default function Page(){const params=useSearchParams(),email=params.get('email')||'',token=params.get('token')||'';const[password,setPassword]=useState(''),[done,setDone]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();const response=await fetch('/api/auth/reinitialiser',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,token,password})});const data=await response.json();if(!response.ok)setError(data.error);else setDone(true)}return <main className="mx-auto max-w-md px-4 py-16"><div className="card p-8"><h1 className="text-2xl font-bold">Nouveau mot de passe</h1>{done?<><p className="mt-4">Votre mot de passe a été modifié.</p><Link href="/auth/connexion" className="btn-primary mt-5">Se connecter</Link></>:<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-medium">Mot de passe<input className="input mt-1" type="password" minLength={10} maxLength={128} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<button className="btn-primary w-full justify-center">Enregistrer</button></form>}</div></main>}
