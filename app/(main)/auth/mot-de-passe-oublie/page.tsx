'use client'
import { FormEvent, useState } from 'react'
import Link from 'next/link'
export default function Page(){const[email,setEmail]=useState(''),[done,setDone]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');const response=await fetch('/api/auth/mot-de-passe-oublie',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const data=await response.json();if(!response.ok)setError(data.error);else setDone(true)}return <main className="mx-auto max-w-md px-4 py-16"><div className="card p-8"><h1 className="text-2xl font-bold">Mot de passe oublié</h1>{done?<p className="mt-4 text-sm">Si ce compte existe, un lien de réinitialisation vient d’être envoyé.</p>:<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-medium">Adresse e-mail<input className="input mt-1" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<button className="btn-primary w-full justify-center">Envoyer le lien</button></form>}<Link className="mt-5 block text-center text-sm text-nihary-or" href="/auth/connexion">Retour à la connexion</Link></div></main>}
