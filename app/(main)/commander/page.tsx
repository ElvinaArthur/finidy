import { BookOpenCheck, ShieldCheck, Truck } from "lucide-react";
import PrintOrderForm from "@/components/commande/PrintOrderForm";

export default async function CommanderPage({ searchParams }: { searchParams: Promise<{ type?: string; id?: string; titre?: string }> }) {
  const params = await searchParams; const typeProduit = params.type === "MAGAZINE" ? "MAGAZINE" : "REVUE"; const titreProduit = params.titre?.slice(0, 200) || (typeProduit === "REVUE" ? "SAONTSY — édition papier" : "Magazine FINIDY — édition papier");
  return <main><section className="border-b border-nihary-sable-fonce bg-nihary-gradient"><div className="mx-auto max-w-5xl px-4 py-12 text-center sm:px-6 lg:py-16"><span className="eyebrow">Bibliothèque physique FINIDY</span><h1 className="mt-3 font-display text-4xl font-bold text-nihary-ambre-fonce sm:text-5xl">Recevez nos publications en version papier</h1><p className="mx-auto mt-4 max-w-2xl text-lg leading-8 text-nihary-brun">Choisissez une finition professionnelle, indiquez votre destination et recevez une proposition personnalisée.</p><div className="mt-7 flex flex-wrap justify-center gap-5 text-xs text-nihary-gris"><span className="flex items-center gap-1.5"><BookOpenCheck size={15} className="text-nihary-or" />Dos carré ou relié</span><span className="flex items-center gap-1.5"><Truck size={15} className="text-nihary-or" />Livraison à confirmer</span><span className="flex items-center gap-1.5"><ShieldCheck size={15} className="text-nihary-or" />Aucun paiement immédiat</span></div></div></section><div className="mx-auto max-w-3xl px-4 py-10 sm:px-6"><PrintOrderForm typeProduit={typeProduit} referenceId={params.id} titreProduit={titreProduit} /></div></main>;
}
