import { CrmStatut, StatutCommandePapier, StatutDemande } from "@prisma/client"; import { NextRequest, NextResponse } from "next/server"; import { requireAdmin } from "@/lib/auth/admin"; import { cleanString, isRecord } from "@/lib/api-validation"; import { prisma } from "@/lib/prisma";
export async function PATCH(req:NextRequest){if(!await requireAdmin())return NextResponse.json({error:"Accès refusé"},{status:403});try{const body:unknown=await req.json();if(!isRecord(body)||typeof body.id!=="string"||!["contact","consultance","commande"].includes(String(body.type)))return NextResponse.json({error:"Requête invalide"},{status:400});const notes=body.notes===""?null:body.notes?cleanString(body.notes,5000):undefined;if(body.type==="contact"){if(typeof body.statut!=="string"||!Object.values(CrmStatut).includes(body.statut as CrmStatut))return NextResponse.json({error:"Statut invalide"},{status:400});return NextResponse.json({success:true,item:await prisma.contactMessage.update({where:{id:body.id},data:{statut:body.statut as CrmStatut,...(notes!==undefined?{notes}:{})}})})}if(body.type==="commande"){if(typeof body.statut!=="string"||!Object.values(StatutCommandePapier).includes(body.statut as StatutCommandePapier))return NextResponse.json({error:"Statut invalide"},{status:400});return NextResponse.json({success:true,item:await prisma.commandePapier.update({where:{id:body.id},data:{statut:body.statut as StatutCommandePapier,...(notes!==undefined?{notes}:{})}})})}if(typeof body.statut!=="string"||!Object.values(StatutDemande).includes(body.statut as StatutDemande))return NextResponse.json({error:"Statut invalide"},{status:400});return NextResponse.json({success:true,item:await prisma.demandeConsultance.update({where:{id:body.id},data:{statut:body.statut as StatutDemande,...(notes!==undefined?{notes}:{})}})})}catch(error){console.error("PATCH /api/admin/crm",error);return NextResponse.json({error:"Mise à jour impossible"},{status:500})}}
