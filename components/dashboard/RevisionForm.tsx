'use client'
import {FormEvent,useState}from'react'
import{useRouter}from'next/navigation'
export default function RevisionForm({article}:{article:{id:string;titre:string;resume:string;motsClés:string[]}}){const router=useRouter();const[form,setForm]=useState({titre:article.titre,resume:article.resume,motsClés:article.motsClés.join(', ')});const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const response=await fetch('/api/revue/revision',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:article.id,titre:form.titre,resume:form.resume,motsClés:form.motsClés.split(',').map((v)=>v.trim()).filter(Boolean)})});const data=await response.json();if(!response.ok){setError(data.error);setLoading(false);return}router.push('/dashboard');router.refresh()}return <form onSubmit={submit} className="card p-6 space-y-5"><div><label className="block text-sm mb-1">Titre</label><input className="input" required value={form.titre} onChange={(e)=>setForm({...form,titre:e.target.value})}/></div><div><label className="block text-sm mb-1">Résumé révisé</label><textarea className="input min-h-72" required value={form.resume} onChange={(e)=>setForm({...form,resume:e.target.value})}/></div><div><label className="block text-sm mb-1">Mots-clés</label><input className="input" value={form.motsClés} onChange={(e)=>setForm({...form,motsClés:e.target.value})}/></div>{error&&<p className="text-red-700">{error}</p>}<button className="btn-primary" disabled={loading}>{loading?'Renvoi…':'Renvoyer au comité'}</button><p className="text-xs text-nihary-gris">Après renvoi, le document sera de nouveau verrouillé et replacé en attente.</p></form>}
